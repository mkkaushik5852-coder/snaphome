# favicon.png

`web/favicon.png` is a binary PNG from the standard Flutter web template and
could not be authored as text in this environment. Running `flutter create .`
inside the `snaphome/` project (locally or in CI) regenerates it. The app
builds and renders fine without it; the browser simply shows no favicon.
